==========================================================================
                    🚀 NGINX性能优化 - 立即部署
==========================================================================

服务器密码: AAbb123456789

==========================================================================
第一步: 上传文件
==========================================================================

在PowerShell中执行（复制粘贴）:

scp nginx.conf deploy@110.40.129.184:/tmp/ && scp server_deploy.sh deploy@110.40.129.184:/tmp/

（会提示输入密码两次，都输入: AAbb123456789）

==========================================================================
第二步: SSH连接并部署
==========================================================================

ssh deploy@110.40.129.184

（输入密码: AAbb123456789）

==========================================================================
第三步: 在服务器上执行（复制整行）
==========================================================================

chmod +x /tmp/server_deploy.sh && /tmp/server_deploy.sh

==========================================================================
或者使用一键命令（在服务器上执行）:
==========================================================================

sudo cp /etc/nginx/conf.d/my_site.conf /etc/nginx/conf.d/my_site.conf.backup.$(date +%Y%m%d_%H%M%S) && sudo cp /tmp/nginx.conf /etc/nginx/conf.d/my_site.conf && sudo nginx -t && sudo systemctl reload nginx && curl -H "Accept-Encoding: gzip" -I http://110.40.129.184/static_data.json | grep -i content-encoding

==========================================================================
成功标志
==========================================================================

看到以下输出表示成功:
  ✓ Content-Encoding: gzip

然后访问 http://110.40.129.184/ 测试速度！

==========================================================================
